\* ty / TLC config for Snapshot.tla (aterm CONSISTENCY family).
CONSTANT MaxSeq = 4
CONSTANT MaxLines = 2
CONSTANT MaxVal = 2
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT SnapAtBounded
INVARIANT SnapIsPrefix
\* The bounded spine terminates at MaxSeq; that is by design, not a deadlock.
CHECK_DEADLOCK FALSE
