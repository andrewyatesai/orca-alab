\* ty / TLC config for Kernel.tla (aterm CONSISTENCY family).
CONSTANT MaxSeq = 8
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT SeqIsLen
INVARIANT Monotonic
\* The bounded spine terminates at MaxSeq; that terminal state is by design, not
\* a deadlock (stutter is allowed by [][Next]_vars).
CHECK_DEADLOCK FALSE
