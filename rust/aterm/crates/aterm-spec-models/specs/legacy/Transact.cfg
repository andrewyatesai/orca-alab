\* ty / TLC config for Transact.tla (aterm CONSISTENCY family).
CONSTANT MaxSeq = 6
CONSTANT NOps = 2
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT SpineGapFree
INVARIANT NoPartialCommit
INVARIANT NoLostUpdate
\* The bounded spine terminates at MaxSeq; that is by design, not a deadlock.
CHECK_DEADLOCK FALSE
