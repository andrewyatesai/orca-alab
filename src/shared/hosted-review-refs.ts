// Logic moved to the Rust hosted-review-refs core (orca-dispatch); this module
// had no types or data to retain, so it is now an import-safe stub. Consumers
// call the normalizers via the napi wrapper (src/main/rust-hosted-review-refs.ts)
// or the wasm wrapper (src/renderer/src/lib/git-wasm/hosted-review-refs.ts).
export {}
