# GPU software-fallback latch TS↔Rust differential parity corpus (E1 unit).
#
# ONE shared crash trace, replayed by BOTH implementations against a fresh tracker
# configured window=30ms, threshold=3:
#   - Rust: orca-crash-recovery::gpu_fallback (include_str! this file)
#   - TS:   GpuCrashFallbackTracker (src/main/crash-reporting/gpu-crash-fallback-decision.ts)
# Divergence on any step fails one side.
#
# Format: `crash <msSinceLaunch> => <shouldEngageFallback:0|1> <crashesInWindow>`
#
# Trace exercises: INCLUSIVE window boundaries (0 and 30 count, 31 and -1 do not),
# the one-shot latch (engages exactly when the count first reaches 3), and that
# every crash after engaging is a no-op (relaunch at most once).
crash 0 => 0 1
crash 30 => 0 2
crash 31 => 0 2
crash -1 => 0 2
crash 15 => 1 3
crash 20 => 0 3
crash 0 => 0 3
