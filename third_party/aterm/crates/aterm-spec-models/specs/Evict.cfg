\* ty / TLC config for Evict.tla (aterm CONSISTENCY family).
CONSTANT MaxSeq = 6
CONSTANT Cap = 3
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT LenBounded
INVARIANT EvictOldestContiguous
INVARIANT CursorBounded
INVARIANT NoSilentLoss
INVARIANT GapJustified
\* The bounded model terminates; that is by design, not a deadlock.
CHECK_DEADLOCK FALSE
