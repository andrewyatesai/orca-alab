# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 The aterm Authors
[package]
name = "aterm-ext"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Extension registry with a trust-tier gate (ATERM_DESIGN WS-D): register named handlers; dispatch only when the caller presents an aterm-cap capability of sufficient tier."

[dependencies]
aterm-cap = { workspace = true }
