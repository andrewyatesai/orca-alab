\* ty / TLC config for WriteAll.tla (aterm DURABILITY family).
CONSTANT Size = 5
CONSTANT Buggy = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT OffBounded
INVARIANT NoSilentDrop
\* The bounded model terminates; that is by design, not a deadlock.
CHECK_DEADLOCK FALSE
